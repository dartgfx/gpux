name: wgpu
description: Dart FFI bindings for wgpu. Metal, Vulkan, DX12.
version: 0.1.0
repository: https://github.com/dartgfx/gpux
topics:
  - gpu
  - webgpu
  - graphics
  - ffi

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  ffi: ^2.1.0
  gpuweb: ^0.1.0
  hooks: ^1.0.0
  code_assets: ^1.0.0
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  ffigen: ^18.1.0
  lints: ^5.0.0
  test: ^1.24.0
