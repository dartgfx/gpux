name: naga
description: WGSL validation and shader compilation for Dart.
version: 0.1.0
repository: https://github.com/dartgfx/gpux
topics:
  - gpu
  - wgsl
  - shader
  - webgpu

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  ffi: ^2.1.0
  hooks: ^1.0.2
  native_toolchain_rust: ^1.0.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
